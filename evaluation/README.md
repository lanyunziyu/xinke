# 购房资金方案自动评测系统

## 概述

这是一个全自动的评测系统，用于验证购房资金方案生成的准确性。评测系统会自动运行测试用例，验证Agent生成的资金方案是否符合预期，并生成详细的评测报告。

## 评测内容

评测系统验证以下几个关键方面的准确性：

### 1. 资金计算准确性
- ✅ **首付金额计算**：验证首付金额是否按照正确的比例计算（首套30%、二套50%等）
- ✅ **贷款金额计算**：验证贷款金额是否正确
- ✅ **月供计算**：验证月供金额是否在合理范围内（考虑利率波动）

### 2. 税费计算准确性
- ✅ **契税率判断**：
  - 90平米以下首套房：1%
  - 90-140平米首套房：1.5%
  - 140平米以上或二套房：3%
- ✅ **个税判断**：是否正确识别"满五唯一"免征个税
- ✅ **增值税判断**：是否正确识别"满两年"免征增值税

### 3. 政策适用性判断
- ✅ **购房资格审查**：非京籍是否需要60个月社保
- ✅ **首付比例判断**：首套、二套的首付比例是否正确
- ✅ **贷款政策判断**：组合贷的结构是否合理

### 4. 方案完整性
- ✅ **信息完整度**：是否包含所有必要信息（首付、月供、税费等）
- ✅ **替代建议**：不符合购房资格时是否提供替代方案
- ✅ **风险提示**：是否包含必要的风险提示

## 测试用例

评测系统包含以下测试用例：

| 用例ID | 场景 | 验证重点 |
|--------|------|---------|
| case_001 | 北京朝阳区首套房纯商贷 | 基础首付、月供、满五唯一税费 |
| case_002 | 北京海淀区首套房组合贷 | 组合贷结构、公积金+商贷月供、满二不满五税费 |
| case_003 | 北京朝阳区二套房 | 二套房首付50%、契税3% |
| case_004 | 北京西城区小户型首套房 | 90平以下契税1%、等额本金、不满两年税费 |
| case_005 | 非京籍购房资格审查 | 60个月社保要求、替代建议 |

## 快速开始

### 1. 安装依赖

```bash
# 确保已安装所有依赖
pip install -r requirements.txt
```

### 2. 配置环境变量

确保 `.env` 文件中配置了必要的环境变量：

```bash
OPENAI_API_KEY=your_api_key_here
OPENAI_API_BASE_URL=https://your-api-endpoint.com  # 可选
OPENAI_MODEL=Qwen3-Max  # 默认模型
```

### 3. 运行评测

```bash
# 进入评测目录
cd evaluation

# 运行完整评测
python housing_finance_evaluator.py
```

### 4. 查看结果

评测完成后，系统会：
1. 在终端打印评测摘要
2. 生成详细的JSON报告（保存在 `evaluation/reports/` 目录）

## 评测报告说明

### 终端输出示例

```
================================================================================
购房资金方案评测报告
================================================================================
评测时间: 2026-01-21T10:30:45.123456
评测耗时: 125.67秒

测试用例总数: 5
通过用例: 4 (80.0%)
失败用例: 1

验证点总数: 20
通过验证点: 17 (85.0%)
平均得分: 82.5分

================================================================================
详细结果:
================================================================================

✓ [case_001] 北京朝阳区首套房纯商贷
  得分: 100.0% (5/5)

✓ [case_002] 北京海淀区首套房组合贷
  得分: 90.0% (9/10)

✓ [case_003] 北京朝阳区二套房
  得分: 95.0% (4/4)

✗ [case_004] 北京西城区小户型首套房
  得分: 60.0% (3/5)
  失败的验证点:
    - 契税率是否正确（90-140平米首套1.5%）: 契税率不匹配: 实际3%，期望1.5%
    - 不满两年需缴纳增值税和个税: 税费义务识别不完整: 未提及增值税, 未提及个税

✓ [case_005] 非京籍购房资格审查
  得分: 85.0% (5/6)

================================================================================
🎉 评测通过！购房资金方案准确性良好。
================================================================================
```

### JSON报告结构

```json
{
  "timestamp": "2026-01-21T10:30:45.123456",
  "duration_seconds": 125.67,
  "total_cases": 5,
  "passed_cases": 4,
  "failed_cases": 1,
  "pass_rate": 80.0,
  "total_validation_points": 20,
  "passed_validation_points": 17,
  "validation_pass_rate": 85.0,
  "average_score": 82.5,
  "results": [
    {
      "test_id": "case_001",
      "name": "北京朝阳区首套房纯商贷",
      "passed": true,
      "score": 100.0,
      "total_points": 5,
      "passed_points": 5,
      "validation_results": [...],
      "agent_response": "...",
      "iterations": 3
    }
  ]
}
```

## 评分规则

### 用例得分计算

```
用例得分 = (通过的验证点数 / 总验证点数) × 100
```

### 及格标准

- **单个用例及格线**：80分
- **整体评测及格线**：80%的用例通过

### 验证点权重

所有验证点权重相等，每个验证点要么完全通过（1分）要么完全失败（0分）。

## 验证逻辑说明

### 1. 精确匹配（exact_match）

用于验证精确数值，如首付金额、贷款金额等。

**示例**：
```python
{
  "key": "down_payment",
  "validation_type": "exact_match",
  "tolerance": 0  # 允许的误差范围（这里允许1万元误差）
}
```

**验证逻辑**：
- 从Agent响应中提取数值
- 与期望值比较
- 误差在容忍范围内即通过

### 2. 范围验证（range）

用于验证月供等受利率影响的数值。

**示例**：
```python
{
  "key": "monthly_payment",
  "validation_type": "range",
  "tolerance": 0.1  # 允许10%的波动
}
```

**验证逻辑**：
- 提取实际月供金额
- 检查是否在预期范围内
- 考虑利率波动因素

### 3. 布尔验证（boolean）

用于验证是否满足某个条件，如税费豁免、购房资格等。

**示例**：
```python
{
  "key": "tax_exemptions",
  "validation_type": "boolean"
}
```

**验证逻辑**：
- 检查响应中是否包含相关关键词
- 判断是否正确识别税费豁免条件

### 4. 百分比验证（percentage）

用于验证比例，如首付比例。

**示例**：
```python
{
  "key": "down_payment_ratio",
  "validation_type": "percentage",
  "expected_value": 0.5  # 50%
}
```

### 5. 关键词匹配（keyword_match）

用于验证是否包含必要的信息说明。

**示例**：
```python
{
  "key": "reason_explanation",
  "validation_type": "keyword_match",
  "keywords": ["60个月", "社保", "不符合"]
}
```

**验证逻辑**：
- 检查响应中是否包含70%以上的关键词
- 用于验证解释的完整性

### 6. 存在性验证（exists）

用于验证是否提供了某类信息。

**示例**：
```python
{
  "key": "alternative_provided",
  "validation_type": "exists"
}
```

### 7. 结构验证（structure）

用于验证复杂结构，如组合贷的组成。

**示例**：
```python
{
  "key": "loan_structure",
  "validation_type": "structure"
}
```

## 添加新测试用例

### 1. 编辑测试用例文件

编辑 `housing_finance_testcases.json`，添加新的测试用例：

```json
{
  "test_id": "case_006",
  "name": "测试用例名称",
  "description": "场景描述",
  "input": {
    "user_query": "用户查询内容",
    "house_info": {
      // 房屋信息
    }
  },
  "expected_output": {
    "down_payment": {
      "amount": 2000000,
      "description": "首付200万"
    },
    // 其他期望输出
  },
  "validation_points": [
    {
      "key": "验证点key",
      "description": "验证点描述",
      "validation_type": "验证类型",
      // 其他配置
    }
  ]
}
```

### 2. 运行评测

```bash
python housing_finance_evaluator.py
```

## 常见问题

### Q1: 评测失败，但我觉得Agent的回答是对的？

**A**: 可能是以下原因：
1. **提取逻辑不够完善**：评测器使用正则表达式提取数值，可能无法识别某些格式
2. **容忍度设置不合理**：可以调整 `tolerance` 参数
3. **期望值设置错误**：检查测试用例中的 `expected_output` 是否正确

**解决方法**：
- 查看详细的JSON报告，了解具体失败原因
- 调整提取逻辑或期望值
- 增加更多的关键词匹配模式

### Q2: 如何调整容忍度？

**A**: 在测试用例的验证点中设置 `tolerance` 参数：

```json
{
  "key": "monthly_payment",
  "validation_type": "range",
  "tolerance": 0.15  // 允许15%的误差
}
```

### Q3: 评测耗时太长怎么办？

**A**: 可以采取以下措施：
1. 减少测试用例数量（先运行核心用例）
2. 降低Agent的 `max_iterations` 参数
3. 使用更快的模型（如 `haiku`）
4. 并行运行测试用例（需要修改代码）

### Q4: 如何只运行特定的测试用例？

**A**: 修改 `housing_finance_evaluator.py` 的 `_load_testcases` 方法，添加过滤逻辑：

```python
def _load_testcases(self) -> List[Dict[str, Any]]:
    with open(self.testcases_file, 'r', encoding='utf-8') as f:
        testcases = json.load(f)

    # 只运行特定ID的测试用例
    target_ids = ['case_001', 'case_002']
    testcases = [tc for tc in testcases if tc['test_id'] in target_ids]

    return testcases
```

## 扩展评测系统

### 添加新的验证类型

在 `HousingFinanceEvaluator` 类中添加新的验证方法：

```python
def _validate_custom_type(self, key: str, response: str, expected: Dict, vp: Dict) -> Dict:
    """自定义验证逻辑"""
    # 实现验证逻辑
    pass
```

然后在 `_validate_point` 方法中注册新的验证类型：

```python
elif validation_type == 'custom_type':
    result = self._validate_custom_type(key, response, expected, validation_point)
```

### 添加评测指标

可以扩展 `_generate_summary` 方法，添加更多统计指标：

```python
# 计算各个类别的通过率
category_stats = {}
for result in self.results:
    category = result.get('category', 'default')
    if category not in category_stats:
        category_stats[category] = {'total': 0, 'passed': 0}
    category_stats[category]['total'] += 1
    if result['passed']:
        category_stats[category]['passed'] += 1
```

## 最佳实践

### 1. 定期运行评测

建议在以下情况运行评测：
- 修改Agent逻辑后
- 更新工具实现后
- 调整Prompt后
- 发布新版本前

### 2. 版本控制

将评测报告纳入版本控制：
```bash
git add evaluation/reports/*.json
git commit -m "Add evaluation report for version X.Y.Z"
```

### 3. 持续改进

- 根据评测结果识别薄弱环节
- 针对失败的用例优化Agent
- 定期增加新的测试用例
- 更新期望输出以反映政策变化

### 4. 文档更新

- 记录每次重大改进
- 更新测试用例说明
- 维护常见问题列表

## 贡献指南

欢迎贡献新的测试用例和验证逻辑！

1. Fork 项目
2. 创建特性分支 (`git checkout -b feature/new-testcase`)
3. 提交更改 (`git commit -m 'Add new test case'`)
4. 推送到分支 (`git push origin feature/new-testcase`)
5. 创建 Pull Request

## 许可证

MIT License
